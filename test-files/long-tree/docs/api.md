# Api

The API exposes four endpoints. Each takes JSON and returns JSON.

## What you need to know

The API exposes four endpoints. Each takes JSON and returns JSON. Read `src/api.rs` for the contract it holds.

